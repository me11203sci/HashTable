// Author: Melesio Albavera
// Date: 25 October 2022
// File: HashTable.h
// Purpose: To contain function prototypes and their respective documentation.

/*
 * Purpose: Implements a dynamic associative array (dictionary) using a
 * 	hash function to compute a unique index for each key, which allows
 * 	for the look up of the corresponding value. Collisions are handled
 * 	by building a linked list at the duplicated hash.
 */

#ifndef HASHTABLE_H
#define HASHTABLE_H

template <typename T>
class HashTable;

template <typename T>
class HashTable
{};

#include "HashTable.hpp"
#endif
